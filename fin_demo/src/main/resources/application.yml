spring:
  devtools:
    restart:
      enabled: true
    livereload: # view?? ???? ? ???? ??? ??
      enabled: true

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: a40844084

  jpa:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQL10Dialoect
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
